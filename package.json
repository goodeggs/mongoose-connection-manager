{
  "name": "mongoose-connection-manager",
  "version": "1.0.2",
  "main": "lib/index",
  "repository": {
    "type": "git",
    "url": "http://github.com/goodeggs/mongoose-connection-manager.git"
  },
  "dependencies": {
    "mongoose": "*"
  },
  "devDependencies": {
    "coffee-script": "^1.7",
    "expect.js": "*",
    "mocha": "*",
    "sinon": "*"
  },
  "scripts": {
    "build": "./node_modules/.bin/coffee -c -o lib src",
    "prepublish": "npm run build",
    "pretest": "npm run build",
    "test": "./node_modules/.bin/mocha"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
